# Telegram Mini App - Генератор изображений с AI

Telegram Mini App для генерации изображений с использованием AI-моделей через OpenRouter API. Приложение поддерживает различные режимы генерации, систему балансов, реферальную программу и платежную систему.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Структура проекта](#структура-проекта)
- [Возможности](#возможности)
- [Быстрая установка](#быстрая-установка)
- [Конфигурация](#конфигурация)
- [API Endpoints](#api-endpoints)
- [База данных](#база-данных)
- [Система оплаты](#система-оплаты)
- [Администрирование](#администрирование)
- [Разработка](#разработка)

---

## 🎯 Описание проекта

Это полнофункциональное Telegram Mini App для генерации изображений с помощью AI. Приложение использует модели Google Gemini через OpenRouter API и предоставляет пользователям различные режимы генерации:

- **Генерация по текстовому промпту** — создание изображений по описанию
- **Генерация с референсом** — создание изображений на основе загруженного фото
- **Фотосессия с продуктом** — размещение продукта в профессиональной сцене
- **Генерация поз** — создание различных поз на основе фото человека
- **Генерация по двум референсам** — комбинирование стиля и объекта

---

## 📁 Структура проекта

```
telegram-mini-app/
├── server.js                 # Точка входа приложения
├── db.js                     # Модуль работы с базой данных MySQL
├── prompts.js                # Управление промптами и моделями AI
├── package.json              # Зависимости проекта
├── .env                      # Переменные окружения (не в git)
├── config.json               # Конфигурация модели (создается автоматически)
│
├── src/
│   ├── app.js                # Создание Express приложения
│   │
│   ├── routes/
│   │   └── api.js            # Все API endpoints
│   │
│   ├── handlers/
│   │   ├── generation.js     # Обработчики генерации изображений
│   │   └── telegramWebhook.js # Обработчик webhook от Telegram
│   │
│   ├── services/
│   │   ├── ai.js             # Интеграция с OpenRouter API
│   │   ├── telegram.js       # Отправка сообщений в Telegram
│   │   ├── billing.js        # Система списания средств
│   │   └── payment.js         # Платежная система (СБП, криптовалюта)
│   │
│   ├── middleware/
│   │   ├── adminGuard.js     # Защита админских endpoints
│   │   └── telegramWebhookGuard.js # Валидация webhook от Telegram
│   │
│   └── utils/
│       ├── logger.js          # Система логирования
│       └── telegram.js        # Утилиты для работы с Telegram
│
└── public/
    └── index.html            # Frontend Mini App (HTML/CSS/JS)
```

### Основные модули

- **`server.js`** — запускает Express сервер на порту из `.env`
- **`db.js`** — управление MySQL подключением, создание таблиц, работа с пользователями
- **`prompts.js`** — управление AI-моделями и промптами для генерации
- **`src/app.js`** — настройка Express: CORS, body parser, статические файлы, роутинг
- **`src/routes/api.js`** — все REST API endpoints приложения

---

## ✨ Возможности

### 🎨 Режимы генерации изображений

1. **Фото по промпту** (`/generate`)
   - Генерация по текстовому описанию
   - Поддержка загрузки референсного изображения
   - Одно изображение на запрос

2. **Фотосессия с продуктом** (`/product-gen`)
   - Генерация 5 вариантов размещения продукта
   - Профессиональная фотография продукта
   - Настройка окружения и стиля

3. **Случайные позы** (`/poses-gen-image`)
   - Генерация от 1 до 10 поз на основе фото человека
   - Сохранение идентичности человека
   - Различные динамические позы

4. **Фото по референсу (2 фото)** (`/generate-refpair`)
   - Первое фото — стиль/композиция
   - Второе фото — объект/человек
   - Комбинирование стиля и объекта

### 💰 Система балансов

- Баланс пользователя в виртуальной валюте (BNB)
- Автоматическое списание средств за генерацию
- Разные цены для разных моделей:
  - `google/gemini-2.5-flash-image`: 10 BNB
  - `google/gemini-3-pro-image-preview`: 25 BNB

### 🎁 Реферальная система

- Уникальный реферальный код для каждого пользователя
- Бонусы при регистрации по реферальной ссылке:
  - Рефереру: +10 BNB
  - Новому пользователю: +5 BNB
- Отслеживание количества приглашенных пользователей

### 💳 Платежная система

- **СБП (Система быстрых платежей)**
  - Минимальная сумма: 100 рублей
  - Максимальная сумма: 50,000 рублей
  - QR-код для оплаты

- **Криптовалюты**
  - Поддержка USDT, BTC, ETH
  - Автоматический расчет эквивалента
  - Генерация адреса кошелька

### 🔐 Администрирование

- Просмотр всех пользователей с рефералами
- Установка баланса пользователя
- Защита через `ADMIN_TOKEN`

---

## 🚀 Быстрая установка

### Требования

- Node.js 16+ 
- MySQL 5.7+ или 8.0+
- Telegram Bot Token
- OpenRouter API Key

### Шаги установки

1. **Клонируйте репозиторий** (если есть) или используйте текущую директорию

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   
   Создайте файл `.env` в корне проекта:
   ```env
   # Порт сервера
   PORT=4000

   # OpenRouter API ключ (обязательно)
   OPENROUTER_API_KEY=sk-or-v1-your-key-here

   # Telegram Bot Token (обязательно)
   TELEGRAM_BOT_TOKEN=your-bot-token-here

   # ID модели по умолчанию
   MODEL_ID=google/gemini-3-pro-image-preview

   # Настройки базы данных MySQL
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=your-password
   DB_NAME=telegram_mini_app

   # Админ токен (для админских endpoints)
   ADMIN_TOKEN=your-secure-admin-token

   # Настройки СБП (опционально)
   SBP_PHONE=+79991234567
   SBP_ACCOUNT=40817810099910004312

   # Адреса криптокошельков (опционально)
   CRYPTO_USDT_WALLET=0x...
   CRYPTO_BTC_WALLET=1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa
   CRYPTO_ETH_WALLET=0x...
   ```

4. **Создайте базу данных:**
   ```sql
   CREATE DATABASE telegram_mini_app CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

   Таблицы создадутся автоматически при первом запуске.

5. **Запустите сервер:**
   ```bash
   node server.js
   ```

   Или с автоперезагрузкой (если установлен nodemon):
   ```bash
   npm run dev
   ```

6. **Настройте Telegram Bot:**
   - Создайте бота через [@BotFather](https://t.me/BotFather)
   - Получите токен и добавьте в `.env`
   - Настройте webhook: `https://your-domain.com/api/telegram-webhook`
   - Создайте Mini App через BotFather и укажите URL вашего приложения

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательно | По умолчанию |
|-----------|----------|-------------|--------------|
| `PORT` | Порт сервера | Нет | 4000 |
| `OPENROUTER_API_KEY` | API ключ OpenRouter | Да | - |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Да | - |
| `MODEL_ID` | ID модели по умолчанию | Нет | `google/gemini-3-pro-image-preview` |
| `DB_HOST` | Хост MySQL | Нет | localhost |
| `DB_PORT` | Порт MySQL | Нет | 3306 |
| `DB_USER` | Пользователь MySQL | Да | - |
| `DB_PASSWORD` | Пароль MySQL | Да | - |
| `DB_NAME` | Имя базы данных | Да | - |
| `ADMIN_TOKEN` | Токен для админских endpoints | Нет | - |
| `SBP_PHONE` | Телефон для СБП | Нет | - |
| `SBP_ACCOUNT` | Счет для СБП | Нет | - |
| `CRYPTO_*_WALLET` | Адреса криптокошельков | Нет | - |

### Управление моделями (config.json)

Файл `config.json` создается автоматически и хранит текущую выбранную модель:

```json
{
  "modelId": "google/gemini-3-pro-image-preview"
}
```

**Доступные модели** (определены в `prompts.js`):
- `google/gemini-3-pro-image-preview` — Nano Banana Pro (25 BNB)
- `google/gemini-2.5-flash-image` — Nano Banana (10 BNB)

**Изменение модели:**
- Через API: `PUT /api/settings` с `{ "modelId": "..." }`
- Вручную: отредактируйте `config.json` или установите `MODEL_ID` в `.env`

### Настройка промптов

Промпты для генерации находятся в `prompts.js`. Вы можете изменить их для достижения нужного стиля:

- `PROMPT_GEN_TEXT` — генерация только по тексту
- `PROMPT_GEN_WITH_IMAGE` — генерация с референсом
- `PROMPT_PRODUCT` — фотосессия продукта
- `PROMPT_POSES` — генерация поз
- `PROMPT_REF_PAIR` — генерация по двум референсам

### Настройка цен

Цены за генерацию настраиваются в `src/services/billing.js`:

```javascript
const MODEL_PRICES = {
    'google/gemini-2.5-flash-image': 10,
    'google/gemini-3-pro-image-preview': 25
};
```

### Настройка платежей

Лимиты платежей в `src/services/payment.js`:

```javascript
const MIN_AMOUNT = 100;   // Минимальная сумма в рублях
const MAX_AMOUNT = 50000; // Максимальная сумма
const EXCHANGE_RATE = 1;  // Курс: 1 рубль = 1 BNB
```

---

## 🔌 API Endpoints

### Публичные endpoints

#### `GET /api/health`
Проверка состояния сервера и наличия ключей.

**Ответ:**
```json
{
  "ok": true,
  "openrouter": "ok",
  "telegram": "ok",
  "timestamp": "2026-02-19T12:00:00.000Z"
}
```

#### `GET /api/settings`
Получить текущую модель и список доступных моделей.

**Ответ:**
```json
{
  "modelId": "google/gemini-3-pro-image-preview",
  "availableModels": [
    { "id": "google/gemini-3-pro-image-preview", "name": "Nano Banana Pro" },
    { "id": "google/gemini-2.5-flash-image", "name": "Nano Banana" }
  ]
}
```

#### `PUT /api/settings`
Изменить модель генерации.

**Тело запроса:**
```json
{
  "modelId": "google/gemini-2.5-flash-image"
}
```

#### `POST /api/generate`
Генерация изображения по текстовому промпту.

**Тело запроса:**
```json
{
  "prompt": "Красивый закат над морем",
  "initData": "user=...",
  "imageBase64": "data:image/jpeg;base64,..." // опционально
}
```

**Ответ:**
```json
{
  "imageUrl": "https://...",
  "sentToChat": true
}
```

#### `POST /api/generate-image`
Генерация с загрузкой изображения через multipart/form-data.

**Form Data:**
- `prompt` (string) — текстовый промпт
- `initData` (string) — Telegram initData
- `image` (file) — изображение (до 50MB)

#### `POST /api/product-gen`
Генерация 5 вариантов фотосессии продукта.

**Тело запроса:**
```json
{
  "prompt": "Профессиональная фотосессия на белом фоне",
  "initData": "user=...",
  "imageBase64": "data:image/jpeg;base64,..."
}
```

**Ответ:**
```json
{
  "imageUrls": ["https://...", "https://..."],
  "sentToChat": true
}
```

#### `POST /api/product-gen-image`
Аналогично `/product-gen`, но с загрузкой файла через form-data.

#### `POST /api/poses-gen-image`
Генерация поз (1-10 штук).

**Form Data:**
- `prompt` (string, опционально) — описание поз
- `initData` (string) — Telegram initData
- `count` (number) — количество поз (1-10)
- `image` (file) — фото человека

#### `POST /api/generate-refpair`
Генерация по двум референсам.

**Тело запроса:**
```json
{
  "prompt": "Применить стиль первого фото ко второму",
  "initData": "user=...",
  "refImageBase64": "data:image/jpeg;base64,...",
  "targetImageBase64": "data:image/jpeg;base64,..."
}
```

#### `GET /api/balance`
Получить баланс пользователя.

**Query параметры:**
- `initData` (string) — Telegram initData

**Ответ:**
```json
{
  "balance": 100,
  "refCode": "NB123ABC",
  "referredCount": 5
}
```

### Платежные endpoints

#### `POST /api/payment/sbp`
Создать платеж через СБП.

**Тело запроса:**
```json
{
  "initData": "user=...",
  "amount": 500
}
```

**Ответ:**
```json
{
  "success": true,
  "payment": {
    "paymentId": "sbp_...",
    "amountRub": 500,
    "amountBNB": 500,
    "method": "sbp",
    "status": "pending",
    "sbpData": {
      "qrCode": "https://qr.nspk.ru/...",
      "phone": "+79991234567",
      "account": "40817810099910004312",
      "comment": "Пополнение баланса #..."
    }
  }
}
```

#### `POST /api/payment/crypto`
Создать платеж через криптовалюту.

**Тело запроса:**
```json
{
  "initData": "user=...",
  "amount": 1000,
  "cryptoType": "USDT"
}
```

#### `GET /api/payment/verify`
Проверить статус платежа.

**Query параметры:**
- `paymentId` (string)
- `initData` (string)

#### `GET /api/payment/history`
Получить историю платежей.

**Query параметры:**
- `initData` (string)

#### `GET /api/payment/limits`
Получить лимиты и настройки платежей.

**Ответ:**
```json
{
  "minAmount": 100,
  "maxAmount": 50000,
  "supportedMethods": ["sbp", "crypto"],
  "supportedCrypto": ["USDT", "BTC", "ETH"]
}
```

### Telegram Webhook

#### `POST /api/telegram-webhook`
Webhook для получения обновлений от Telegram.

Обрабатывает команды `/start` и обычные сообщения.

### Админские endpoints

Требуют параметр `token` в query или заголовке `x-admin-token`.

#### `GET /api/admin/users?token=...`
Получить список всех пользователей с рефералами.

**Ответ:**
```json
{
  "users": [
    {
      "id": 1,
      "telegram_user_id": "123456",
      "username": "user",
      "balance": 100,
      "ref_code": "NB123ABC",
      "referred_count": 5,
      "created_at": "2026-02-19T12:00:00.000Z"
    }
  ]
}
```

#### `POST /api/admin/set-balance?token=...`
Установить баланс пользователя.

**Тело запроса:**
```json
{
  "telegram_user_id": "123456",
  "balance": 500
}
```

---

## 🗄️ База данных

### Схема таблиц

#### Таблица `users`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT AUTO_INCREMENT | Первичный ключ |
| `telegram_user_id` | VARCHAR(64) UNIQUE | ID пользователя Telegram |
| `chat_id` | VARCHAR(64) | ID чата для отправки сообщений |
| `username` | VARCHAR(255) | Username пользователя |
| `first_name` | VARCHAR(255) | Имя |
| `last_name` | VARCHAR(255) | Фамилия |
| `balance` | INT DEFAULT 0 | Баланс в BNB |
| `ref_code` | VARCHAR(64) UNIQUE | Реферальный код |
| `referred_by` | VARCHAR(64) | Код реферера |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата обновления |

#### Таблица `referrals`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INT AUTO_INCREMENT | Первичный ключ |
| `referrer_user_id` | VARCHAR(64) | ID реферера |
| `referred_user_id` | VARCHAR(64) | ID приглашенного |
| `created_at` | TIMESTAMP | Дата создания |

### Автоматическое создание

Таблицы создаются автоматически при первом вызове `initDb()`. Не требуется ручное создание схемы.

---

## 💳 Система оплаты

### Текущая реализация

Платежная система реализована как заглушка с базовой логикой. Для продакшена необходимо:

1. **Интеграция с платежным провайдером:**
   - Для СБП: ЮKassa, CloudPayments, или другой провайдер
   - Для криптовалют: интеграция с блокчейном или платежным шлюзом

2. **Создание таблицы `payments`:**
   ```sql
   CREATE TABLE payments (
       id INT AUTO_INCREMENT PRIMARY KEY,
       payment_id VARCHAR(255) UNIQUE,
       telegram_user_id VARCHAR(64),
       amount_rub DECIMAL(10,2),
       amount_bnb INT,
       method VARCHAR(20),
       status VARCHAR(20),
       created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
       updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
   );
   ```

3. **Реализация проверки платежей:**
   - Webhook от платежного провайдера
   - Периодическая проверка статуса
   - Автоматическое зачисление средств

### Настройка платежей

Измените значения в `src/services/payment.js`:

```javascript
const MIN_AMOUNT = 100;      // Минимальная сумма
const MAX_AMOUNT = 50000;    // Максимальная сумма
const EXCHANGE_RATE = 1;     // Курс конвертации
```

---

## 🔐 Администрирование

### Получение админского токена

Установите `ADMIN_TOKEN` в `.env`:

```env
ADMIN_TOKEN=your-secure-random-token-here
```

### Использование админских endpoints

Добавьте параметр `token` в query:

```
GET /api/admin/users?token=your-secure-random-token-here
```

Или используйте заголовок:

```
x-admin-token: your-secure-random-token-here
```

### Управление пользователями

**Просмотр всех пользователей:**
```bash
curl "http://localhost:4000/api/admin/users?token=YOUR_TOKEN"
```

**Установка баланса:**
```bash
curl -X POST "http://localhost:4000/api/admin/set-balance?token=YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"telegram_user_id": "123456", "balance": 1000}'
```

---

## 🛠️ Разработка

### Структура кода

- **Модульность:** Каждый функционал вынесен в отдельный модуль
- **Логирование:** Используется `debugLog()` для отладки
- **Обработка ошибок:** Try-catch блоки во всех критических местах
- **Валидация:** Проверка входных данных перед обработкой

### Добавление новой модели

1. Добавьте модель в `prompts.js`:
   ```javascript
   const AVAILABLE_MODELS = [
       // ... существующие модели
       { id: 'new/model-id', name: 'Новая модель' }
   ];
   ```

2. Добавьте цену в `src/services/billing.js`:
   ```javascript
   const MODEL_PRICES = {
       // ... существующие цены
       'new/model-id': 30
   };
   ```

### Добавление нового режима генерации

1. Создайте новый промпт в `prompts.js`
2. Добавьте обработчик в `src/handlers/generation.js`
3. Добавьте endpoint в `src/routes/api.js`
4. Обновите frontend в `public/index.html`

### Логирование

Все важные события логируются через `debugLog()` из `src/utils/logger.js`. Логи выводятся в консоль с временными метками и структурированными данными.

### Тестирование

Для тестирования используйте:

- **Health check:** `GET /api/health`
- **Локальный webhook:** Используйте ngrok для тестирования Telegram webhook
- **Прямые запросы:** Используйте Postman или curl для тестирования API

---

## 📝 Лицензия

Проект создан для внутреннего использования. Все права защищены.

---

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи сервера
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к базе данных
4. Проверьте валидность API ключей

---

## 📚 Дополнительные ресурсы

- [OpenRouter API Documentation](https://openrouter.ai/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Express.js Documentation](https://expressjs.com/)

---

**Версия:** 1.0.0  
**Последнее обновление:** Февраль 2026
